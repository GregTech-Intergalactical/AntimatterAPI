pluginManagement {
    repositories {
        gradlePluginPortal()

        maven { url = 'https://files.axelandre42.ovh/maven/' }
        maven { url = 'https://files.minecraftforge.net/maven/' }

        jcenter()
        mavenCentral()
        maven { url 'http://dvs1.progwml6.com/files/maven' }

    }
}

includeBuild('tesseract/forge') {

}

//Not actually needed (i think) but it creates proper module dependency in Idea
includeBuild('tesseract') {
    dependencySubstitution {
        substitute module('com.github.gregtech-intergalactical:tesseract') with project(':')
    }
}
